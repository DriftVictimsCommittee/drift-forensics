# Reference #202

## Subject

Drift Protocol multisig pre-migration on-chain checks Proof-of-Concept

## Source URL

[https://github.com/DriftVictimsCommittee/drift-forensics/tree/main/nonce_check_test](https://github.com/DriftVictimsCommittee/drift-forensics/tree/main/nonce_check_test)


## Technical Implementation Details

This Proof-of-Concept (PoC) demonstrates that the presence of unauthorized Durable Nonce accounts could have been detected using standard Solana RPC methods prior to the March 25, 2026 multisig migration.

The detection logic (`src/lib.rs`) performs a query against the **System Program** with the following filters:

1.  **DataSize Filter**: Restricts results to accounts of exactly 80 bytes (the fixed size of a Solana Nonce Account).
2.  **State Discriminator**: Checks for an initialized state (`[1, 0, 0, 0]` at offset 0).
3.  **Authority Match**: Uses `Memcmp` to filter accounts where the Authority field (offset 8) matches the public key of a Security Council member.

This process requires no specialized indexing or off-chain data, only basic read access to the blockchain state.

## Extracted Code Logic

> **Note:** The following Rust code snippet represents the core detection function used in the PoC. It iterates through council members and identifies any nonce accounts they control that are not explicitly whitelisted.

```rust
pub fn check_durable_nonces(
    rpc_client: &RpcClient,
    council_members: Vec<Pubkey>,
    whitelisted_nonce_accounts: Vec<Pubkey>,
) -> Result<Vec<(Pubkey, Pubkey)>, Box<dyn std::error::Error>> {
    let mut warn_nonce_accounts = Vec::new();

    for council_member in council_members {
        // Filters to identify valid, initialized Durable Nonce accounts owned by the council member:
        // 1. DataSize(80): Standard size of a NonceAccount.
        // 2. Memcmp(0, [1,0,0,0]): Discriminator/State == Initialized (u32 little-endian).
        // 3. Memcmp(8, <pubkey>): Authority field offset at byte 8.
        let filters = vec![
            // 1. Datasize filter
            RpcFilterType::DataSize(80),
            // 2. Discriminator filter == 1 (Initialized)
            RpcFilterType::Memcmp(Memcmp::new_raw_bytes(0, vec![1, 0, 0, 0])),
            // 3. Authority pubkey filter
            RpcFilterType::Memcmp(Memcmp::new_base58_encoded(8, &council_member.to_bytes())),
        ];

        let config = RpcProgramAccountsConfig {
            filters: Some(filters),
            account_config: RpcAccountInfoConfig {
                encoding: Some(UiAccountEncoding::Base64),
                // Fetch only the first 40 bytes (Discriminator + State + Authority) to minimize bandwidth.
                data_slice: Some(UiDataSliceConfig {
                    offset: 0,
                    length: 40,
                }),
                commitment: Some(CommitmentConfig::confirmed()),
                min_context_slot: None,
            },
            with_context: Some(false),
            sort_results: Some(true),
        };

        // Query the System Program for matching accounts.
        let accounts =
            rpc_client.get_program_ui_accounts_with_config(&SYSTEM_PROGRAM_ID, config)?;

        // Filter out whitelisted accounts and collect warnings.
        for (nonce_pubkey, _nonce_account) in accounts {
            if !whitelisted_nonce_accounts.contains(&nonce_pubkey) {
                warn_nonce_accounts.push((nonce_pubkey, council_member));
            }
        }
    }

    Ok(warn_nonce_accounts)
}
```

## Test Execution Output

> Note: The following output was generated by running cargo test against a local validator instance pre-loaded with the state of the Drift Protocol prior to the exploit.

```bash
running 1 test
Scanning for durable nonces controlled by council members...
Found 1 suspicious nonce account(s):
  Nonce: 7s7s6saC5LHZoLyBXLM3pCjpWaA7meyQdP8NiH9ktAeC controlled by: 39JyWrdbVdRqjzw9yyEjxNtTbTKcTPLdtdCgbz7C7Aq8
test durable_nonce_check ... ok

test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 0.17s
```

## Conclusion

The PoC confirms that the "red flag" nonce account `7s7s6saC5LHZoLyBXLM3pCjpWaA7meyQdP8NiH9ktAeC` with `nonce_authority` set to `39JyWrdbVdRqjzw9yyEjxNtTbTKcTPLdtdCgbz7C7Aq8` was technically detectable via simple RPC queries. 

The Drift Protocol team had the technical capability to identify this compromise indicator before retaining the associated council member in the new multisig configuration on March 25, 2026.