# Reference #104

## Subject

`Controlled` Multisignature wallet

## Description

Drift Protocol uses SQUADS for multisignature wallet implementation.

## SQUADS Protocol description

Squads Protocol is a collection of programs (smart contracts) that enables developers to use smart account technology to build novel asset management and self-custody solutions.

Due to its permissionless nature, developers can seamlessly integrate Squads Protocol's programs into their own applications, enabling enhanced features that leverage multi-signature functionality and Solana's account abstraction capabilities. These features include spending limits, sub-accounts, time locks, and more.

## Controlled multisig description

In Squads V4, `Config Authority` - is the authority that can change the multisig config.
The convention is to set this to `Pubkey::default()` - n this case, the multisig becomes autonomous, so every config change goes through the normal process of voting by the members. 

However, if this parameter is set to any other key, all the config changes for this multisig will need to be signed by the `config_authority`.

This means a single account - the `Config Authority` - can unilaterally add/remove members or change threshold/timelock **without a quorum** of multisig members.

## Controlled multisig public warnings

The Squads Protocol documentation itself explicitly warns against this setup:

```
Controlled Multisigs

Understanding Config Authority with Squads

Using a Controlled Multisig is not recommended for most use-cases. Please understand the tradeoffs of having a valid Config Authority before choosing to use this setup.

What is a Controlled Multisig?

To put it simply, a Controlled Multisig is a multisig with a set Config Authority. This allows the set config authority to manipulate the config of the Squad without being subject to consensus, causing the set key to essentially function as an admin.

Why use a Config Authority?

Any setups that require a single admin for config changes are suited for a Controlled Multisig. If you are an organization that requires a C-level executive to have specific config control, then a controlled multisig may be in order.
```

Source: [https://docs.squads.so/main/development/reference/controlled-multisigs](https://docs.squads.so/main/development/reference/controlled-multisigs)

## Additional notes

### SQUADS Documentation & codebase source:

- [https://docs.squads.so/main/development/reference/controlled-multisigs](https://docs.squads.so/main/development/reference/controlled-multisigs)

- [https://docs.squads.so/main/development/reference/accounts#multisig-account-content](https://docs.squads.so/main/development/reference/accounts#multisig-account-content)

- [https://github.com/agouin/squads-v4/blob/main/programs/squads_multisig_program/src/instructions/multisig_config.rs](https://github.com/agouin/squads-v4/blob/main/programs/squads_multisig_program/src/instructions/multisig_config.rs)

